# XI visualizer

- [`src/graphics`](src/graphics/README.md) — the rendering layer: coordinates, viewer lifecycle,
  performance rules. Read this before touching anything that draws.
- [`scripts`](scripts/README.md) — headless rendering (`shot.mjs`) and the rest of the tooling.
